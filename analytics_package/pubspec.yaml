name: analytics_package
description: Mobile analytics SDK for Flutter with persistent queue and retry delivery.
version: 0.1.0
homepage:

environment:
  sdk: ^3.10.4
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  meta: ^1.16.0
  shared_preferences: ^2.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  http: ^1.2.2

flutter:
